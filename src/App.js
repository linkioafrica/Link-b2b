import { Button } from "./components/button/Button";

const App = () => {
  return (
    <div>
      <Button
        bgColor="bg-blue-500"
        ringColor="ring-blue-200"
        text="Hello"
        textSize="text-lg"
      />
    </div>
  );
};

export default App;
